import { ImageButtonDirective } from './image-button.directive';

describe('ImageButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageButtonDirective();
    expect(directive).toBeTruthy();
  });
});
